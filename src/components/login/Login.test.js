import React from 'react';
import Login from '../../components/login/Login'

describe('Test case for testing loginform', () => {
	test('username check', () => {;
		const component = ( < Login / > );


	})

});